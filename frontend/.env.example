# API origin — leave empty for local dev (Vite proxies /api to localhost:4000).
# Production (Vercel): set to your Render API URL, e.g. https://ozone-attendance-api.onrender.com
# VITE_API_URL=

# Google Maps JavaScript API key — optional locally (backend serves key via /api/settings/public).
# Required on Vercel for production map previews (embedded at build time).
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
